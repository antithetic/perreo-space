# TODO

Items collected from `TODO` comments in the repo.

## Content model

- [ ] **Artist genres and subgenres** — add genre/subgenre fields and surface them on the artist page.
  - `packages/sanity/src/schema/documents/artist.ts`
  - Commented-out `genres` reference field is already sketched.

- [ ] **Event location field** — consider bringing location back for virtual events, or in-person events without a strict venue. Venue already has location.
  - `packages/sanity/src/schema/documents/event.ts`
  - Commented-out `location` text field is already sketched.
